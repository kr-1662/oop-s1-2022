#include <iostream>
using namespace std;

void dangerous_values(double vals[],double threshold, int length) {
    for (int i = 0; i < length; i++) {
        if (vals[i] < threshold) {

        }
    }
}